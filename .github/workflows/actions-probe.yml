name: Actions Probe

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Probe
        run: |
          echo "ACTIONS_PROBE_OK"
          echo "SHA=$GITHUB_SHA"
          echo "EVENT=$GITHUB_EVENT_NAME"
